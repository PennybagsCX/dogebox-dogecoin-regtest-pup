# Dogecoin Core Regtest pup — source

This directory is the pup source (`manifest.json` + `pup.nix`). The repo root carries [`dogebox.json`](../dogebox.json), which makes the whole repo usable as a dogeboxd pup-store source.

**Full documentation**: [root README](../README.md) — install, configuration, usage, changelog.
